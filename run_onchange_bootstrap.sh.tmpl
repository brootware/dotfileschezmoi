#!/bin/bash

# .bootstrap/setup.yml hash: {{ include "dotbootstrap/setup.yml" | sha256sum }}

if command -v ansible-playbook &> /dev/null; then
    ansible-playbook {{ joinPath .chezmoi.sourceDir "dotbootstrap/setup.yml" | quote }} --ask-become-pass
fi